<UserControl x:Class="CarryIQ.App.ClubManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Margin="0,0,20,0" Padding="20" Background="#FFFFFF" BorderBrush="#D9E2EC" BorderThickness="1" CornerRadius="18">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
                    <TextBlock Text="Club bag" FontSize="24" FontWeight="SemiBold" Foreground="#102A43" />
                    <TextBlock Text="Active clubs in user-defined order" Margin="0,6,0,0" Foreground="#627D98" />
                    <Border Margin="0,12,0,0" Padding="12" Background="#FFF7ED" BorderBrush="#FDBA74" BorderThickness="1" CornerRadius="10">
                        <TextBlock Text="{Binding Editor.ValidationMessage}" Foreground="#9A3412" TextWrapping="Wrap" />
                    </Border>
                    <TextBlock Text="{Binding ErrorMessage}" Margin="0,12,0,0" Foreground="#B91C1C" TextWrapping="Wrap" />
                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <Button Content="New" Command="{Binding NewCommand}" Padding="14,8" Margin="0,0,8,0" />
                        <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="14,8" />
                    </StackPanel>
                </StackPanel>

                <ListBox x:Name="ClubsList"
                         ItemsSource="{Binding Clubs}"
                         SelectedItem="{Binding SelectedClub, Mode=TwoWay}"
                         SelectionChanged="ClubsList_SelectionChanged"
                         BorderThickness="0"
                         Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12" Margin="0,0,0,10" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="#102A43" />
                                    <TextBlock Margin="0,4,0,0" Foreground="#627D98"
                                               Text="{Binding ClubType}" />
                                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                        <TextBlock Foreground="#52606D" Text="Order " />
                                        <TextBlock Foreground="#334E68" Text="{Binding SortOrder}" />
                                        <TextBlock Foreground="#52606D" Text="  " />
                                        <TextBlock Foreground="#52606D" Text="{Binding IsActive, StringFormat=Active: {0}}" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Padding="24" Background="#FFFFFF" BorderBrush="#D9E2EC" BorderThickness="1" CornerRadius="18">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Edit club" FontSize="24" FontWeight="SemiBold" Foreground="#102A43" />
                    <TextBlock Text="Loft is optional. Saving an active club enforces unique names within the bag." Margin="0,6,0,18" Foreground="#627D98" TextWrapping="Wrap" />
                    <Border Margin="0,0,0,16" Padding="12" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
                        <StackPanel>
                            <TextBlock Text="Editor status" FontWeight="SemiBold" Foreground="#102A43" />
                            <TextBlock Text="{Binding Editor.ValidationMessage}" Margin="0,6,0,0" Foreground="#52606D" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <UniformGrid Columns="2" Margin="0,0,0,12">
                        <StackPanel Margin="0,0,12,12">
                            <TextBlock Text="Name" Foreground="#334E68" />
                            <TextBox Text="{Binding Editor.Name, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Club type" Foreground="#334E68" />
                            <ComboBox ItemsSource="{Binding Editor.ClubTypes}"
                                      SelectedItem="{Binding Editor.ClubType}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,12,12">
                            <TextBlock Text="Loft" Foreground="#334E68" />
                            <TextBox Text="{Binding Editor.Loft, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Length (yards)" Foreground="#334E68" />
                            <TextBox Text="{Binding Editor.LengthYards, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,12,12">
                            <TextBlock Text="Manufacturer" Foreground="#334E68" />
                            <TextBox Text="{Binding Editor.Manufacturer, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Model" Foreground="#334E68" />
                            <TextBox Text="{Binding Editor.Model, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,12,12">
                            <TextBlock Text="Shaft" Foreground="#334E68" />
                            <TextBox Text="{Binding Editor.Shaft, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Shaft flex" Foreground="#334E68" />
                            <TextBox Text="{Binding Editor.ShaftFlex, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </UniformGrid>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Notes" Foreground="#334E68" />
                        <TextBox Text="{Binding Editor.Notes, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="90" TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <CheckBox Content="Active" IsChecked="{Binding Editor.IsActive}" Margin="0,0,16,0" />
                        <TextBlock VerticalAlignment="Center" Foreground="#627D98"
                                   Text="{Binding Editor.SortOrder, StringFormat=Sort order: {0}}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                        <Button Content="Save" Command="{Binding SaveCommand}" Padding="16,8" Margin="0,0,8,0" />
                        <Button Content="Deactivate" Command="{Binding DeactivateCommand}" Padding="16,8" Margin="0,0,8,0" />
                        <Button Content="Move up" Command="{Binding MoveUpCommand}" Padding="16,8" Margin="0,0,8,0" />
                        <Button Content="Move down" Command="{Binding MoveDownCommand}" Padding="16,8" />
                    </StackPanel>

                    <Border Margin="0,20,0,0" Padding="16" Background="#F8FAFC" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Audit" FontWeight="SemiBold" Foreground="#102A43" />
                            <TextBlock Text="{Binding Editor.CreatedAt, StringFormat=Created: {0:u}}" Margin="0,8,0,0" Foreground="#52606D" />
                            <TextBlock Text="{Binding Editor.UpdatedAt, StringFormat=Updated: {0:u}}" Margin="0,4,0,0" Foreground="#52606D" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
