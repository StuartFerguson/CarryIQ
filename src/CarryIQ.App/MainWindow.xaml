<Window x:Class="CarryIQ.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:CarryIQ.App"
        Title="CarryIQ"
        Width="1320"
        Height="860"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Padding="24" CornerRadius="18" Background="#102A43">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{Binding Title}" FontSize="34" FontWeight="SemiBold" Foreground="White" />
                    <TextBlock Text="{Binding Subtitle}" Margin="0,8,0,0" FontSize="16" Foreground="#D9E2EC" />
                    <TextBlock Text="{Binding StatusMessage}" Margin="0,18,0,0" FontSize="14" Foreground="#BCCCDC" TextWrapping="Wrap" />
                </StackPanel>

                <Border Grid.Column="1" Padding="14" Background="#16324F" CornerRadius="12" BorderBrush="#243B53" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Database" FontWeight="SemiBold" Foreground="#F0F4F8" />
                        <TextBlock Text="{Binding DatabasePath}" Margin="0,6,0,0" TextWrapping="Wrap" Foreground="#D9E2EC" Width="320" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <controls:ClubManagerView Grid.Row="1" Margin="0,18,0,0" DataContext="{Binding ClubManager}" />
    </Grid>
</Window>
