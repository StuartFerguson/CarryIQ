<DynamicComponent Type="@ShellScreenComponentMap.Resolve(Screen?.GetType())"
                  Parameters="@(new Dictionary<string, object?> { ["ViewModel"] = Screen! })" />

@code {
    [Parameter]
    public IShellScreenViewModel? Screen { get; set; }
}
