# Contributing

Keep changes focused, tested, and documented.

Before opening a pull request:

1. Run `dotnet restore`
2. Run `dotnet build CarryIQ.sln -c Release`
3. Run `dotnet test CarryIQ.sln -c Release`

